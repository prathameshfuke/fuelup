import { PitStopLog } from '@/components/pit-stop';

export const metadata = {
  title: 'Pit Stop Log | FuelUp F1',
  description: 'F1 pit stop logging interface with real-time metrics',
};

export default function PitStopPage() {
  return <PitStopLog />;
}
